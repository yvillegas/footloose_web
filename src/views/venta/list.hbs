<div class="container">
    <div class="row mt-5">
        <h3 class="text-end m-0"> <a href="/ventas/add" class="btn btn-dark">AGREGAR</a></h3>
        <h3 class="text-center text-uppercase p-2">Lista de ventas </h3>
        {{#if venta}}
        <table class="table text-center">
            <thead>
                <tr class="table-light">
                    <th scope="col" class="col-4">Fecha</th>
                    <th scope="col" class="col-4">Cliente</th>
                    <th scope="col" class="col-4">Importe Total</th>
                    <th scope="col">Acciones</th>
                </tr>
            </thead>
            <tbody>
                {{#each venta}}
                <tr>
                    <td>{{fecha_formateada}}</td>
                    <td>{{name}}</td>
                    <td>{{totalVenta}}</td>
                    <td>
                        <div class="btn-group">
                            <a href="/detalleventas/list/{{idVenta}}" class="btn btn-success p-2">
                                <i class="fa-solid fa-bars"></i></a>
                            <a href="/ventas/edit/{{idVenta}}" class="btn btn-warning p-2">
                                <i class="fa-solid fa-pencil"></i></a>
                            <a href="/ventas/delete/{{idVenta}}" onclick="return confirm('Seguro que desea eliminar?')"
                                class="btn btn-danger p-2">
                                <i class="fa-solid fa-trash"></i></a>
                        </div>
                    </td>
                </tr>
                {{/each}}
            </tbody>
            </tfoot>
        </table>
    </div>
    {{else}}
    <div class="card text-center mx-auto" style="width: 350px;">
        <div class="card-header">
            <h3>Debes crear una venta</h3>
        </div>
        <div class="card-body">
            <p>Presiona este boton para que puedas crear una marca</p>
            <a href="/ventas/add" class="btn btn-primary">Ir a crear</a>
        </div>
        {{/if}}
    </div>
</div>