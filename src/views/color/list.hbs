<div class="container">
    <div class="row mt-5">
        <h3 class="text-end m-0"> <a href="/colores/add" class="btn btn-dark">AGREGAR</a></h3>
        <h3 class="text-center text-uppercase p-2">Lista de colores</h3>
        {{#if color}}
        <table class="table text-center">
            <thead>
                <tr class="table-light">
                    <th scope="col" class="col-4">Nombre</th>
                    <th scope="col">Acciones</th>
                </tr>
            </thead>
            <tbody>
                {{#each color}}
                <tr>
                    <td>{{nombreColor}}</td>
                    <td>
                        <div class="btn-group">
                            <a href="/colores/edit/{{idColor}}" class="btn btn-warning p-2">
                                <i class="fa-solid fa-pencil"></i></a>
                            <a href="/colores/delete/{{idColor}}" onclick="return confirm('Seguro que desea eliminar?')"
                                class="btn btn-danger p-2">
                                <i class="fa-solid fa-trash"></i></a>
                        </div>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
    {{else}}
    <div class="card text-center mx-auto" style="width: 350px;">
        <div class="card-header">
            <h3>Debes crear una Color</h3>
        </div>
        <div class="card-body">
            <p>Presiona este boton para que puedas crear una Color</p>
            <a href="/colores/add" class="btn btn-primary">Ir a crear</a>
        </div>
        {{/if}}
    </div>
</div>